const salaJS = ["Evaldo", "Camis", "Mari"];
const salaPython = ["Ju", "Leo", "Raquel"];

const salasUnificadas = salaJS.concat(salaPython); // Juntando dois arrays. Lembrando que a ordem em que foi declarada a junção, será respeitada

console.log(salasUnificadas);